import React from "react";
import { ScrollView, View, Text } from "react-native";

const Writing = () => {
  return (
    <ScrollView>
      <View>
        <Text>Writing</Text>
      </View>
    </ScrollView>
  );
};

export default Writing;
