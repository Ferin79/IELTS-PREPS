import React from "react";
import { ScrollView, View, Text } from "react-native";

const Reading = () => {
  return (
    <ScrollView>
      <View>
        <Text>Reading</Text>
      </View>
    </ScrollView>
  );
};

export default Reading;
